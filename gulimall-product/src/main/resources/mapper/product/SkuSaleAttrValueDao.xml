<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.zjut.product.dao.SkuSaleAttrValueDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="cn.edu.zjut.product.entity.SkuSaleAttrValueEntity" id="skuSaleAttrValueMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>

    <resultMap type="cn.edu.zjut.product.vo.SkuItemSaleAttrVO" id="SkuItemSaleAttrVOMap">
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <collection property="attrValues" ofType="cn.edu.zjut.product.vo.AttrValueWithSkuIdVO">
            <result property="attrValue" column="attr_value"/>
            <result property="skuIds" column="sku_ids"/>
        </collection>
    </resultMap>

    <select id="getSaleAttrsBySpuId" resultMap="SkuItemSaleAttrVOMap">
        select psaav.attr_id, psaav.attr_name, psaav.attr_value, group_concat(distinct psi.sku_id) as sku_ids
        from pms_sku_info psi
                 left join pms_sku_sale_attr_value psaav on psi.sku_id = psaav.sku_id
        where psi.spu_id = #{spuId}
        group by psaav.attr_id, psaav.attr_name, psaav.attr_value
    </select>

    <select id="getSkuSaleAttrValuesAsStringList" resultType="java.lang.String">
        select concat(attr_name, ':', attr_value)
        from pms_sku_sale_attr_value
        where sku_id = #{skuId}
    </select>

</mapper>